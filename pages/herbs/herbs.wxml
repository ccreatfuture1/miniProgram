<view  class="weui-search-bar {{inputShowed ? 'weui-search-bar_focusing' : ''}}" id="searchBar" style="background:white;" >
			<form class="weui-search-bar__form" style="border:3rpx solid #ccc; box-shadow:0px 1px 1px 1px rgb(187, 186, 186); " >
				<view class="weui-search-bar__box">
					<i class="weui-icon-search"></i>
                    <input type="text" class="weui-search-bar__input" placeholder="搜索" value="{{inputVal}}" focus="{{inputShowed}}" bindinput="inputTyping" />
                    <span class="weui-icon-clear" wx:if="{{inputVal.length > 0}}" bindtap="clearInput"></span>
				</view>
				<label class="weui-search-bar__label" bindtap="showInput">
					<i class="weui-icon-search"></i>
           <picker name="num" bindchange="pickerChange" value="{{picker.index}}" range="{{picker.arr}}">
                <span class="weui-search-bar__text"> {{picker.arr[picker.index]}}</span>
           </picker>
           
				</label>
			</form>
            <view class="weui-search-bar__cancel-btn" bindtap="hideInput">取消</view>
		</view>
 
<view class="classify">
  <!--分类导航栏-->
  <!-- <scroll-view class='left-navbar' scroll-y="true">
    <view wx:for="{{screenArray}}" class="{{ currentTab == index ? 'active' : '' }}" wx:key="unique" 
     bindtap="{{ currentTab != index ? 'navbarTap':'' }}" id="{{index}}" 
      data-screenId="{{item.screenId}}">
       {{item.screenName}}
    </view>
  </scroll-view> -->
  <scroll-view id="right" scroll-y="true" scroll-top="{{scrollTop}}">
  
    <!--顶部药品展示图片-->
    <view class="goods-banner">
      <image class="img-banner" src="{{herbs[picker.index].img}}"  mode="scaleToFill"></image>
    </view>
    <!--药品介绍-->
    <view class="name">
      <text class="title">名称</text>
      <text>
          {{herbs[picker.index].name}}
      </text>
    </view>
    <view class="from">
       <text class="title">来源</text>
       <text>
           {{herbs[picker.index].from}}
       </text>
    </view>
    <view class="function">
        <text class="title">功能主治</text>
          <text>
           {{herbs[picker.index].functions}}
          </text>
    </view>
    <view class="Processing">
        <text class="title">炮制</text>
          <text>
            {{herbs[picker.index].Processing}}
          </text>
    </view>
    <view class="usage">
         <text class="title">用法用量</text>
         <text>
           {{herbs[picker.index]. usage}}
         </text>
    </view>
  </scroll-view>
 
</view>

